# lineWidth

Fallback line width. Inherited from dprint global lineWidth when unset

|             |                                       |
| ----------- | ------------------------------------- |
| **Type**    | `integer`                             |
| **Default** | *inherited from dprint global config* |

## Config

```json
{
  "svg": {
    "lineWidth": null
  }
}
```
